export const DEFAULT_THEME = {
  primary: "#f72585",
  accent: "#7209b7",
  background: "#fff8fb",
};

export type ThemeColors = {
  primary: string;
  accent: string;
  background: string;
  brand50: string;
  brand100: string;
  brand200: string;
  brand300: string;
  brand400: string;
  brand500: string;
  brand600: string;
  brand700: string;
  accent500: string;
  accent600: string;
};

export type ThemeSettings = {
  primary: string;
  accent?: string;
};
